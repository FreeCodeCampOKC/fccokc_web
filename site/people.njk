---
title: Techlahoma Member Spotlight
description: A list of some notable Techlahoma community members. Feel free to nominate folks to be added to the list, or submit a pull request to add yourself!
layout: layouts/base.njk
head: <link rel="stylesheet" href="/css/people.css">
---

<h1>Techlahoma Member Spotlight</h1>

{%- for person in people | sort(attribute='lastname') -%}
    <div class="person-card">
        <h2>{{person.firstname}} {{person.lastname}}</h2>
        <p>{{person.bio}}</p>
        <footer>
            {%- for contact in person.contact -%}
                {% if contact.link %}
                    <p>
                        <a href="{{contact.url}}">{{contact.text}}</a>
                    </p>
                {% else %}
                    <p>{{contact.text}}: {{contact.url}}</p>
                {% endif %}
            {%- endfor -%}
        </footer>
    </div>
{%- endfor -%}
